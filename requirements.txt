# --- Backend framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.3

# --- RAG orchestration ---
llama-index==0.14.23
llama-index-vector-stores-qdrant==0.10.3
llama-index-llms-ollama==0.10.1
llama-index-embeddings-ollama==0.9.0

# --- Vector DB client ---
qdrant-client==1.19.0

# --- Auth ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# NOTE: bcrypt is pinned below passlib's compatibility ceiling. Newer
# bcrypt releases (4.1+) removed an internal attribute passlib's
# version-detection relies on, causing "password cannot be longer than
# 72 bytes" errors even on short passwords. Don't upgrade bcrypt alone
# without checking passlib compatibility first.

# --- PII redaction ---
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
spacy==3.8.15

# --- Output guardrails ---
guardrails-ai==0.11.0

# --- Config / DB ---
pydantic-settings==2.15.0
sqlalchemy==2.0.52

# --- Frontend ---
streamlit==1.61.1

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# NOTE: after installing, run:
#   python -m spacy download en_core_web_lg
# (required by Presidio's default NER model)
